--CREATE TABLE MediaItems 
--(
--	MID NUMBER(9,0) NOT NULL,
--	TITLE VARCHAR2 (200),
--	PROD_YEAR NUMBER(4),
--	TITLE_LENGTH NUMBER(4),
--	CONSTRAINT MediaItems_PK PRIMARY KEY(MID)
--);
--
--CREATE TABLE Similarity 
--(
--	MID1 NUMBER(9,0) NOT NULL,
--	MID2 NUMBER(9,0) NOT NULL,
--	SIMILARITY FLOAT,
--	CONSTRAINT Similarity_PK PRIMARY KEY
--	(MID1,MID2),    
--	CONSTRAINT FK_MID1MID FOREIGN KEY (MID1) REFERENCES MediaItems(MID),
--	CONSTRAINT FK_MID2MID FOREIGN KEY (MID2) REFERENCES MediaItems(MID)
--);

--CREATE TABLE Users (
--    USERID NUMBER(9,0) primary key,
--    USERNAME varchar2(150),
--    PASSWORD varchar2(150),
--    FIRST_NAME varchar2(150),
--    LAST_NAME varchar2(150),
--    DATE_OF_BIRTH varchar2(150),
--    REGISTRATION_DATE TIMESTAMP
--);
--
--CREATE TABLE Administrators (
--    ADMINID NUMBER(9,0) primary key,
--    USERNAME VARCHAR2(150),
--    PASSWORD VARCHAR2 (150)
--);
--
--CREATE TABLE LoginLog (
--    USERID NUMBER(9,0),
--    LOGINTIME TIMESTAMP,
--    CONSTRAINT LoginLog_PK PRIMARY KEY(USERID, LOGINTIME),
--    CONSTRAINT FK_USERID FOREIGN KEY (USERID) REFERENCES Users(USERID)
--);

CREATE TABLE History (
    USERID NUMBER(9,0),
    MID NUMBER(9,0),
    VIEWTIME TIMESTAMP,
    CONSTRAINT History_PK PRIMARY KEY(USERID, MID, VIEWTIME),
    CONSTRAINT FK_USERID2 FOREIGN KEY (USERID) REFERENCES Users(USERID),
    CONSTRAINT FK_MID2 FOREIGN KEY (MID) REFERENCES MediaItems(MID)
);





